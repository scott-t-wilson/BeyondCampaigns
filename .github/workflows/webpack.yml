name: NodeJS with Webpack

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [16.x]
    
    steps:
    - uses: actions/checkout@v2

    - name: Setup Node.js environment
      uses: actions/setup-node@v2.4.1

    - name: Build webpack
      run: |
        npm install
        npm run-script build

    - name: Build web-ext
      uses: kewisch/action-web-ext@v1.0
      with:
        cmd: build
        source: dist
        verbose: true

  release:
    name: Create GitHub Release
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2

    - name: Create tag
      id: create_tag
      run: |
        tag=builds-$(cat version.txt)
        echo "::set-output name=tag::$tag"

    - name: Create Draft Release
      id: create_release
      uses: actions/create-release@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: ${{ steps.create_tag.outputs.tag }}
        release_name: ${{ steps.create_tag.outputs.tag }}
        draft: true
        prerelease: false

    - uses: actions/upload-release-asset@v1.0.1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        https://addons.mozilla.org/firefox/downloads/file/3821097/beyond_campaigns-1.0-fx.xpi
        upload_url: ${{ steps.create_release.outputs.upload_url }}
        asset_path: beyond_campaigns-${{steps.create_tag.outputs.tag}}.xpi
        asset_name: beyond_campaigns-${{steps.create_tag.outputs.tag}}.xpi
        asset_content_type: application/zip

    - uses: eregon/publish-release@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        release_id: ${{ steps.create_release.outputs.id }}